plugins {
    id("com.android.library")
    id("org.jetbrains.kotlin.android")
}

group = "tech.graaf.franz.ar_flutter_plugin_plus"
version = "1.0-SNAPSHOT"

android {
    namespace = "tech.graaf.franz.ar_flutter_plugin_plus"
    compileSdk = 36

    defaultConfig {
        minSdk = 24
        targetSdk = 36
    }

    sourceSets {
        getByName("main").java.srcDirs("src/main/kotlin")
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17"
        languageVersion = "2.0"
    }

    buildFeatures {
        buildConfig = false
    }
}

dependencies {
    // Kotlin Runtime
    implementation("org.jetbrains.kotlin:kotlin-stdlib-jdk8:2.1.0")

    // Kotlin Coroutines (required for SceneView 2.0+)
    implementation("org.jetbrains.kotlinx:kotlinx-coroutines-core:1.11.0")
    implementation("org.jetbrains.kotlinx:kotlinx-coroutines-android:1.11.0")

    // Google ARCore - 16KB page size compatible
    implementation("com.google.ar:core:1.54.0")
    
    // SceneView 2.0+ - 16KB page size compatible (replaces deprecated Sceneform)
    // This includes Filament rendering engine with 16KB support
    implementation("io.github.sceneview:arsceneview:2.3.0")

    // AndroidX + Play Services
    implementation("androidx.appcompat:appcompat:1.7.0")
    // 2.9.x is the last line compatible with compileSdk 36 / AGP 8.11.1;
    // 2.10+ transitively pulls lifecycle-runtime-compose which requires compileSdk 37 + AGP 9.1.0
    implementation("androidx.lifecycle:lifecycle-runtime-ktx:2.9.4")
    implementation("com.google.android.gms:play-services-auth:21.6.0")
}

// Kotlin toolchain is configured via compileOptions and kotlinOptions above
// Using JAVA_HOME for Java 17 detection
